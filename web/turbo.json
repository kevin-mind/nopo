{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "persistent": true
    },
    "build": {
      "dependsOn": ["db#generate"],
      "outputs": ["build"]
    },
    "setup": {
      "cache": false
    }
  }
}
