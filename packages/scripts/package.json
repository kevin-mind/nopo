{
  "name": "@more/scripts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules",
    "check:lint": "eslint",
    "fix:lint": "eslint --fix",
    "test": "vitest",
    "script": "npm_config_offline=false npm_config_frozen_lockfile=false npx zx --install ./index.js"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "zod": "^3.24.2",
    "zx": "^8.5.2"
  }
}
