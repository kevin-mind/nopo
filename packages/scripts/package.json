{
  "name": "@more/scripts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules",
    "check:lint": "eslint",
    "fix:lint": "eslint --fix",
    "test": "vitest run --run",
    "script": "tsx ./index.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "docker-compose": "^1.2.0",
    "zod": "^3.24.2",
    "zx": "^8.5.2"
  }
}
