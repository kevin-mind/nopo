{
  "name": "{{scope}}/{{name}}",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "files": [
    "build/**"
  ],
  "scripts": {
    "build": "exit 1",
    "clean": "exit 1",
    "dev": "exit 1",
    "check:types": "exit 1",
    "check:lint": "exit 1",
    "fix:lint": "exit 1",
    "test": "exit 1"
  },
  "devDependencies": {
    "@more/configs": "workspace:*"
  }
}
