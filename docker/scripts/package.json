{
  "name": "nopo",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "nopo": "./bin.js"
  },
  "main": "./build/index.js",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "clean": "rm -rf node_modules build",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.16.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "docker-compose": "^1.2.0",
    "zod": "^3.24.2",
    "zx": "^8.5.2"
  },
  "engines": {
    "node": ">=20"
  }
}
