{
  "name": "website",
  "version": "0.0.1",
  "private": true,
  "description": "Project root",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -ws",
    "build": "npm run build -ws",
    "start": "npm run start -ws",
    "typecheck": "npm run typecheck -ws"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
