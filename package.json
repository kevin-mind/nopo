{
  "name": "nopo",
  "version": "0.0.1",
  "private": true,
  "description": "mo(no) re(po) tools for mono repos",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "apps:build": "pnpm --filter \"./apps/*\" build",
    "apps:dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "clean": "pnpm run \"/^clean:.*/\"",
    "clean:workspace": "pnpm run -r clean",
    "clean:root": "rm -rf .cache .config node_modules .bash_history .env",
    "check": "pnpm run \"/^check:.*/\"",
    "check:workspace": "pnpm run -r \"/^check:.*/\"",
    "check:lint:root": "eslint",
    "check:types:root": "tsc --noEmit",
    "fix": "pnpm run \"/^fix:.*/\"",
    "fix:workspace": "pnpm run -r \"/^fix:.*/\"",
    "fix:lint:root": "eslint --fix",
    "dev": "pnpm run -r dev",
    "build": "pnpm run -r build",
    "start": "pnpm run -r start",
    "test": "pnpm run -r test",
    "release": "pnpm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.1",
    "@eslint/compat": "^1.2.8",
    "@eslint/js": "^9.23.0",
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/node": "^20",
    "docker-compose": "^1.2.0",
    "dotenv": "^16.4.7",
    "eslint": "^9.23.0",
    "eslint-config-prettier": "^10.1.1",
    "eslint-plugin-prettier": "^5.2.6",
    "npm-run-all": "^4.1.5",
    "prettier": "3.5.3",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.29.0",
    "znv": "^0.5.0",
    "zod": "^3.24.2",
    "zx": "^8.5.2"
  },
  "packageManager": "pnpm@10.8.1",
  "engines": {
    "node": ">=20.0.0"
  }
}
