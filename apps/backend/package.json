{
  "name": "@more/backend",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "files": [
    "build/**"
  ],
  "scripts": {
    "build": "mkdir -p build && echo 'build' > build/index.html",
    "clean": "exit 0",
    "format": "exit 0",
    "lint": "exit 0",
    "test": "exit 0",
    "dev": "uv run main.py",
    "start": "uv run main.py"
  },
  "devDependencies": {
    "@more/configs": "workspace:*",
    "vitest": "^3.1.1"
  }
}
