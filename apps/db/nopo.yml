name: db
description: PostgreSQL database for persistent data storage.
image: postgres:16
runtime:
  cpu: "1"
  memory: "512Mi"
  port: 5432
  min_instances: 1
  max_instances: 1
  has_database: false
  run_migrations: false

commands:
  test: echo "No tests for db service"
