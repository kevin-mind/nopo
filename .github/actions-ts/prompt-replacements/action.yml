name: Build Prompt Replacements
description: Safely builds JSON for prompt_replacements, escaping special characters

inputs:
  pairs:
    description: 'Newline-separated KEY=VALUE pairs'
    required: true

outputs:
  json:
    description: 'Valid JSON object string'

runs:
  using: node20
  main: dist/index.cjs
