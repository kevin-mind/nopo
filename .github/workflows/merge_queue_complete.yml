name: Merge Queue Completex

on:
  workflow_run:
    workflows: ["Merge Queue"]
    types:
      - completed

permissions: {}

defaults:
  run:
    shell: bash

jobs:
  complete:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/context
