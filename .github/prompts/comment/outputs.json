{
  "type": "object",
  "properties": {
    "action_type": {
      "type": "string",
      "enum": ["response", "implementation"],
      "description": "Whether this was a question/discussion (response) or a request to implement code changes (implementation)"
    },
    "response_body": {
      "type": "string",
      "description": "The comment response to post"
    },
    "commits": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Commit SHAs if implementation was done (only for action_type=implementation)"
    },
    "agent_notes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Important context for future agents"
    }
  },
  "required": ["action_type", "response_body"]
}
